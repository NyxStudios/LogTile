Currently, this is our LogTile integration.  It will( hopefully ) add logging of tile edits and allow for quick reverts and grief management. 

Currently this is a super alpha( logs the edits and puts into a db )

Hopefully in the next few days we can have commands and a better db structure.